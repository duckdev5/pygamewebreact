import React from 'react';
import { useNavigate } from 'react-router-dom';

const Home = () => {
  const navigate = useNavigate();

  // buraya kodlar...
  
  return (
    <div>
      {/* içerik */}
    </div>
  );
};

export default Home;
